---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "planetscale_neki_admin Resource - terraform-provider-planetscale"
subcategory: ""
description: |-
  NekiAdmin Resource
---

# planetscale_neki_admin (Resource)

NekiAdmin Resource



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `branch` (String) Branch name from `list_branches`. Example: `main`. Requires replacement if changed.
- `database` (String) Database name slug from `list_databases`. Example: `app-db`. Requires replacement if changed.
- `organization` (String) Organization name slug from `list_organizations`. Example: `acme`. Requires replacement if changed.

### Optional

- `admin_size` (String) The admin size SKU, e.g. `NKA_0`.
- `parameters` (Map of Map of String) Desired effective parameter values nested by namespace. The SDK hook uses the prior Terraform value for reconciliation and removes this query parameter before sending the API request.

### Read-Only

- `id` (String) The ID of the admin
- `state` (String) The current state of the admin

## Import

Import is supported using the following syntax:

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = planetscale_neki_admin.my_planetscale_neki_admin
  id = jsonencode({
    branch       = "..."
    database     = "..."
    organization = "..."
  })
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import planetscale_neki_admin.my_planetscale_neki_admin '{"branch": "...", "database": "...", "organization": "..."}'
```
