---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "planetscale_postgres_bouncer Data Source - terraform-provider-planetscale"
subcategory: ""
description: |-
  PostgresBouncer DataSource
---

# planetscale_postgres_bouncer (Data Source)

PostgresBouncer DataSource

## Example Usage

```terraform
data "planetscale_postgres_bouncer" "my_postgresbouncer" {
  branch       = "...my_branch..."
  database     = "...my_database..."
  name         = "...my_name..."
  organization = "...my_organization..."
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `branch` (String) Branch name from `list_branches`. Example: `main`.
- `database` (String) Database name slug from `list_databases`. Example: `app-db`.
- `name` (String) The name of the bouncer
- `organization` (String) Organization name slug from `list_organizations`. Example: `acme`.

### Read-Only

- `actor` (Attributes) (see [below for nested schema](#nestedatt--actor))
- `bouncer_size` (String) The bouncer size, e.g. `PGB_5`, `PGB_10`, `PGB_20`, `PGB_40`, `PGB_80`, or `PGB_160`. Defaults to `PGB_5`.
- `id` (String) The ID of the bouncer
- `parameters` (Map of Map of String) PgBouncer parameter overrides, nested by namespace, e.g. { pgbouncer = { default_pool_size = "100" } }. Omitted parameters are reset to their defaults.
- `replicas_per_cell` (Number) The count of replicas in each cell
- `target` (String) The instance type the bouncer targets

<a id="nestedatt--actor"></a>
### Nested Schema for `actor`

Read-Only:

- `id` (String) The ID of the actor
