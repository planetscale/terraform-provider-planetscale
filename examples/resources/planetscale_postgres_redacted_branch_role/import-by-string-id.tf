import {
  to = planetscale_postgres_redacted_branch_role.my_planetscale_postgres_redacted_branch_role
  id = jsonencode({
    branch       = "..."
    database     = "..."
    id           = "..."
    organization = "..."
  })
}
