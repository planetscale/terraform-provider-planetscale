import {
  to = planetscale_postgres_read_only_replica.my_planetscale_postgres_read_only_replica
  id = jsonencode({
    branch       = "..."
    database     = "..."
    name         = "..."
    organization = "..."
  })
}
